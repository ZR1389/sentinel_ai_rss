# Optional FastAPI dependencies for high-performance health check server
# Add these to requirements.txt if you want to use FastAPI instead of Flask

fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Installation:
# pip install -r requirements-fastapi.txt
#
# Then use in railway.toml:
# uvicorn health_check:app --host 0.0.0.0 --port $PORT
#
# Benefits of FastAPI:
# - Higher performance (async)
# - Automatic API documentation
# - Better type validation
# - Modern Python async/await support
#
# Current Flask setup works perfectly for most use cases!
